import Button from 'react-bootstrap/Button';

export const AddToHumidorButton = () => {
    return (
        <div className="add-to-humidor-button">
            <Button variant="success">Add to Humidor</Button>
        </div>
    );
};
